apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.pendingPublisherIssuerNode.deployment.name }}
  labels:
    {{- include "polygon-id-issuer.pendingPublisherIssuerNode.deploymentLabels" . | nindent 4 }}
    {{- include "polygon-id-issuer.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.pendingPublisherIssuerNode.deployment.replicaCount }}
  selector:
    matchLabels:
      {{- include "polygon-id-issuer.pendingPublisherIssuerNode.Labels" . | nindent 6 }}
      {{- include "polygon-id-issuer.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "polygon-id-issuer.pendingPublisherIssuerNode.Labels" . | nindent 8 }}
        {{- include "polygon-id-issuer.selectorLabels" . | nindent 8 }}
    spec:
      initContainers:
        - name: {{ .Values.pendingPublisherIssuerNode.deployment.init1.containerName }}
          image: {{ .Values.pendingPublisherIssuerNode.deployment.init1.image.repository }}:{{ .Values.pendingPublisherIssuerNode.deployment.init1.image.tag }}
          imagePullPolicy: {{ .Values.pendingPublisherIssuerNode.deployment.init1.imagePullPolicy | quote }}
          envFrom:
            - configMapRef:
                name: {{ .Values.pendingPublisherIssuerNode.deployment.init1.apiconfigMapRef }}
          env:
            - name: {{ include "helpers.issuer-db-url" .}}
              value: {{ quote .Values.pendingPublisherIssuerNode.deployment.init1.env.issuerDatabaseUrl }}
            - name: {{ include "helpers.issuer-key-store-addr" .}}
              value: {{ quote .Values.pendingPublisherIssuerNode.deployment.init1.env.issuerKeyStoreAddress }}
          command: {{ .Values.pendingPublisherIssuerNode.deployment.init1.command }}
          volumeMounts: {{- toYaml .Values.pendingPublisherIssuerNode.deployment.init1.volumeMounts | nindent 12 }} 

        - name: {{ .Values.pendingPublisherIssuerNode.deployment.init2.containerName }}
          image: {{ .Values.pendingPublisherIssuerNode.deployment.init2.image.repository }}:{{ .Values.pendingPublisherIssuerNode.deployment.init2.image.tag }}
          imagePullPolicy: {{ .Values.pendingPublisherIssuerNode.deployment.init2.imagePullPolicy | quote }}
          envFrom:
            - configMapRef:
                name: {{ .Values.pendingPublisherIssuerNode.deployment.init2.apiconfigMapRef }}
          env:
            - name: {{ include "helpers.issuer-db-url" .}}
              value: {{ quote .Values.pendingPublisherIssuerNode.deployment.init2.env.issuerDatabaseUrl }}
            - name: {{ include "helpers.issuer-key-store-addr" .}}
              value: {{ quote .Values.pendingPublisherIssuerNode.deployment.init2.env.issuerKeyStoreAddress }}
          command: {{ .Values.pendingPublisherIssuerNode.deployment.init2.command }}
          volumeMounts: {{- toYaml .Values.pendingPublisherIssuerNode.deployment.init2.volumeMounts | nindent 12 }} 

      containers:
        - name: {{ .Values.pendingPublisherIssuerNode.deployment.containerName }}
          image: {{ .Values.pendingPublisherIssuerNode.deployment.image.repository }}:{{ .Values.pendingPublisherIssuerNode.deployment.image.tag }}
          imagePullPolicy: {{ .Values.pendingPublisherIssuerNode.deployment.imagePullPolicy | quote }}
          envFrom:
            - configMapRef:
                name: {{ .Values.apiUiIssuerNode.deployment.apiUi.apiconfigMapRef }}
            - configMapRef:
                name: {{ .Values.apiUiIssuerNode.deployment.init.apiuiconfigMapRef }}
          env:
            - name: {{ include "helpers.issuer-db-url" .}}
              value: {{ quote .Values.pendingPublisherIssuerNode.deployment.env.issuerDatabaseUrl }}
            - name: {{ include "helpers.issuer-key-store-addr" .}}
              value: {{ quote .Values.pendingPublisherIssuerNode.deployment.env.issuerKeyStoreAddress }}
          args: {{ .Values.pendingPublisherIssuerNode.deployment.args }}
          command: {{ .Values.pendingPublisherIssuerNode.deployment.command }}
          volumeMounts: {{- toYaml .Values.pendingPublisherIssuerNode.deployment.volumeMounts | nindent 10 }}
      volumes:
      - name: {{ .Values.apiIssuerNode.pvc.volumeName }} 
        persistentVolumeClaim:
          claimName: {{ .Values.apiIssuerNode.pvc.name }}
      - name: {{ .Values.vaultIssuerNode.pvc.volumeName }}
        persistentVolumeClaim:
          claimName: {{ .Values.vaultIssuerNode.pvc.vault }}